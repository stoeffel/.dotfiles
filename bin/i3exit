#!/bin/sh
case "$1" in
    lock)
        sh ~/.dotfiles/bin/locki3
        ;;
    logout)
        i3-msg exit
        ;;
    suspend)
        sh ~/.dotfiles/bin/locki3 && systemctl suspend
        ;;
    hibernate)
        sh ~/.dotfiles/bin/locki3 && systemctl hibernate
        ;;
    reboot)
        systemctl reboot
        ;;
    shutdown)
        systemctl poweroff
        ;;
    *)
        echo "Usage: $0 {lock|logout|suspend|hibernate|reboot|shutdown}"
        exit 2
esac

exit 0
